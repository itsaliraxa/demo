import api from './api'
import data from './data'
import meta from './meta'
import theme from './theme'

/**
 * API constant values
 */
export const API = {
  ...api
}

/**
 * Data value constant values
 */
export const DATA = {
  ...data
}

/**
 * Project meta constant values
 */
export const META = {
  ...meta
}

/**
 * Theme constant values
 */
export const THEME = {
  ...theme
}
